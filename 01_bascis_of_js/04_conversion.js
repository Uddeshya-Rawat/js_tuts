let myName="udde";

// we can convert the following variable into any datatype according to us.

let toBool=Boolean(myName);
console.log(toBool,typeof toBool);  // true boolean

let toNum=Number(myName);
console.log(toNum,typeof toNum);  // NaN (not a number) number

/* let toNull=Null(myName);
   console.log(toNull, typeof toNull); // error */

 /* let toUndefiend=Undefined(myName);
console.log(toUndefiend,typeof toUndefiend);   // it will show error as both null and undefiend are not defined. */